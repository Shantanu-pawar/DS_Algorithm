package I0I_Concepts_Arr_Strings.DSA_impl_useFunctions;

public class XOR_properties {

    /*
    AND properties

        0 & 1 = 0 // AND मधे 0 ची पावर मोठी
        1 & 1 = 1
        0 & 0 = 0

    OR properties
        0 | 1 = 1 // OR मधे 1 ची पावर मोठी
        1 | 1 = 1
        0 | 0 = 0

    XOR properties that how it basically returns the elements

        0 ^ 0 = 0 // it's combination of AND, OR
        0 ^ 1 = 1
        1 ^ 1 = 0

    in this it basically provide output of non repetitive elements
    but only gives one element if we give two single nums then
    it returns the sum of it. */
}
